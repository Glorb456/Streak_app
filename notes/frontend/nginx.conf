server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # The Streak nginx strips the /notes/ prefix before proxying here, so this
    # server sees plain paths and the SPA fallback is the ordinary one.
    location / {
        try_files $uri /index.html;
    }
}
